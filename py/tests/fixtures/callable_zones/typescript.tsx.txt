type Props = {items: string[]};

export const Component = ({items}: Props): JSX.Element => (
    <button onClick={(event: Event): void => handle(event)}>
        {items.map((item: string): JSX.Element => <span>{item}</span>)}
    </button>
);
